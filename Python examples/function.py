# function = a block of code which is executed only when it is called
# an Argument is the information you're sending to a function
# a matching set of Parameters need to match the the num of Arguments


def hello(first_name,last_name,age):
    print("hello "+first_name+" "+last_name)
    print("you are "+str(age)+" years old")

myfirst_name = "guy"
mylast_name = "lee"



hello(myfirst_name,mylast_name,45)



































